# 홈 화면 개선안 (유저 플로우 관점)

## 1. 현재 홈 구조 요약

| 구역 | 비로그인 | 로그인 (차량 O) | 로그인 (차량 X) |
|------|----------|-----------------|-----------------|
| 인사말 | 게스트 + 로그인하고 검증을 시작해보세요 | OOO님, + 부제목 | 동일 |
| 상단 블록 | **LoginButtons** (로그인/회원가입) | VehicleInfo 카드 | "차량 정보를 등록해주세요" 문구만 |
| 메인 CTA | 견적서 검증하기 + 설명 | 동일 | 동일 |
| 하단 | (없음) | 최근 검증 내역 (3건 또는 빈 상태) | 동일 |

---

## 2. 유저 플로우 관점의 이슈

### 2.1 비로그인 유저

- **이슈**: 상단에 LoginButtons가 크게 있어 "먼저 로그인해야 하나?"로 오인할 수 있음. 실제로는 **로그인 없이도 견적 검증**이 가능함.
- **결과**: 검증 전환율 저하, 불필요한 로그인 시도 증가 가능.

### 2.2 행동 우선순위

- **핵심 행동** = "견적서 검증하기" 한 번 하기.
- 현재는 인사말 → (로그인/차량 블록) → CTA 순이라, **메인 액션이 시선 아래**로 밀림.
- 첫 방문·재방문 모두 "바로 검증"으로 유도하는 구조가 부족함.

### 2.3 로그인 + 차량 없음

- "차량 정보를 등록해주세요"만 있고 **다음 행동(등록하기)** 버튼이 없음.
- 내 차 관리 탭으로 가야 하는지, 홈에서 할 수 있는지 불명확.

### 2.4 재방문·리턴 유저

- 매번 "견적서 검증하기" → 입력 방식 선택으로만 들어가야 함.
- **자주 쓰는 입력 방식**(예: 사진 촬영)으로 가는 숏컷이 없음.
- 최근 검증 1건 요약이나 "방금 검증한 결과 다시 보기" 같은 연결도 없음.

### 2.5 기대치·신뢰

- "무엇을 하면 되고, 얼마나 걸리는지"가 한눈에 안 들어옴.
- 예: "사진/앨범/직접 입력 중 하나로 1분 안에 확인" 같은 문구가 있으면 진입 장벽이 낮아짐.

---

## 3. 개선안 (우선순위별)

### 3.1 [높음] 비로그인 시 메인 액션 우선 노출

**목표**: "바로 검증"이 첫 번째 행동으로 보이게.

- **안 A**: 블록 순서 변경  
  - 인사말 → **견적서 검증하기 CTA** → "로그인 없이도 검증할 수 있어요. 저장하려면 로그인하세요" + LoginButtons(작게 또는 접이식).
- **안 B**: 비로그인일 때만 CTA를 인사말 바로 아래로 올리고, LoginButtons는 CTA 아래로 이동.  
  - 문구 예: "지금 받은 견적, 적정한지 1분 안에 확인해보세요" → CTA → 그 다음 로그인 유도.

**권장**: 안 A 또는 B 적용. 비로그인 홈의 **첫 번째 인터랙티브 블록**을 "견적서 검증하기"로 통일.

---

### 3.2 [높음] 비로그인용 문구 정리

**목표**: "로그인 필수"가 아니라 "검증은 누구나, 저장은 로그인 후"가 드러나게.

- **인사말 부제목**  
  - 현재: "로그인하고 검증을 시작해보세요"  
  - 변경 예: "견적서만 넣으면 적정 가격을 바로 확인해드려요" (행동·가치 강조).
- **LoginButtons 카드**  
  - 제목: "검증 결과를 저장하고 내역을 관리하려면"  
  - 부제: "로그인 없이도 검증은 할 수 있어요"  
  - 버튼: 로그인 / 회원가입 유지.

이렇게 하면 "먼저 로그인"이 아니라 "먼저 검증 → 필요하면 로그인" 플로우가 분명해짐.

---

### 3.3 [높음] 차량 없을 때 다음 행동 제공

**목표**: "차량 정보를 등록해주세요"에서 멈추지 않고, 등록 플로우로 연결.

- **추가**: "차량 등록하기" 버튼 (→ `/vehicle` 또는 차량 등록 모달/페이지).
- **문구**: "차량을 등록하면 검증 결과가 더 정확해져요" 등 (선택).
- **위치**: 현재 차량 안내 카드와 동일한 자리, 버튼을 눈에 띄게.

로그인 유저가 차량 없을 때 **홈에서 한 번에** 다음 행동을 취할 수 있게 함.

---

### 3.4 [중간] 메인 CTA 보조 문구로 기대치 설정

**목표**: "뭘 하는 건지, 얼마나 걸리는지"를 한 줄로.

- **CTA 아래 설명**  
  - 현재: "정비소에서 받은 견적, 적정한지 확인해보세요"  
  - 보완 예: "사진 촬영·앨범·직접 입력 중 선택해서 약 1분 안에 확인할 수 있어요" (입력 방식 + 소요 시간).
- 또는 CTA 위에 짧은 라벨: "1분 만에 견적 적정성 확인" 등.

첫 방문·재방문 모두 "바로 해볼 만하다"는 인상을 주기 좋음.

---

### 3.5 [중간] 빠른 검증 숏컷 (재방문 유저)

**목표**: 단골 유저가 입력 방식 선택 없이 자주 쓰는 경로로 바로 진입.

- **옵션 1**: CTA 아래 또는 옆에 **보조 버튼** 2개  
  - "사진으로 검증" → `/verify/camera`  
  - "직접 입력" → `/verify/manual`  
  - (앨범은 "사진으로 검증"과 같은 흐름이라 생략 가능.)
- **옵션 2**: "견적서 검증하기"는 현재처럼 `/verify`로 두고, `/verify` 페이지에서 "최근에 사용한 방식"을 상단에 노출 (구현은 다음 단계).

홈에서는 옵션 1만 적용해도 재방문 시 클릭 수를 줄일 수 있음.

---

### 3.6 [중간] 최근 검증 내역 영역 정리

**목표**: 정보량은 유지하면서 "더 보기" 플로우 명확화.

- **항목 수**: 최근 3건 유지하되, 1건 이상 있으면 **"검증 내역 전체 보기"** 링크를 섹션 하단에 추가 (→ `/history`).
- **빈 상태**: 현재 문구 유지 또는 "첫 견적을 검증해보세요" + 견적서 검증하기 버튼(작게) 반복 노출.
- **표시**: 카드가 길어지지 않도록 요약 정보(날짜, 금액, 상태 뱃지) 위주 유지.

홈에서는 요약, 상세는 내역 탭으로 보내는 구조를 분명히 함.

---

### 3.7 [낮음] 로그인 유저 맞춤 톤

**목표**: 재방문 시 "다시 와줘서 고마움" + 다음 행동 제안.

- **부제목 변형**:  
  - 최근 검증이 있으면: "오늘도 견적 검증해보세요" / "마지막 검증: N월 N일" 등 (선택).  
  - 없으면: 현재처럼 "오늘도 정비 견적 검증과 함께 하세요".
- **선택**: "정비 상식" 또는 "이번 주 팁" 한 줄을 인사말 아래에 노출해, 정비 상식 탭으로 자연스럽게 유도.

우선순위가 낮으므로, 상위 개선 적용 후 여유 있을 때 도입해도 됨.

---

### 3.8 [낮음] 시각적 위계 정리

**목표**: 메인 CTA가 "홈의 주인공"으로 보이게.

- **CTA**:  
  - 크기·색은 유지하되, 비로그인일 때는 **인사말 바로 아래**에 두어 첫 폴드에 들어오게.  
  - 로그인일 때도 CTA를 차량 카드보다 위에 두거나, 차량 카드와 CTA를 같은 "폴드" 안에 배치.
- **로그인/차량 카드**:  
  - CTA보다 보조 정보로 보이도록 (카드 스타일 유지, 약간 작은 폰트 또는 세로 간격 조정).

레이아웃만 조정해도 "검증하기"가 메인 액션이라는 인상이 강해짐.

---

## 4. 적용 순서 제안

| 단계 | 항목 | 기대 효과 |
|------|------|-----------|
| 1 | 비로그인 시 CTA 상단 이동 + 문구 정리 (3.1, 3.2) | 검증 전환율·진입 경로 명확화 |
| 2 | 차량 없을 때 "차량 등록하기" 버튼 (3.3) | 이탈 감소, 차량 등록 완료율 상승 |
| 3 | CTA 보조 문구(기대치) + 최근 내역 "전체 보기" (3.4, 3.6) | 이해도·내역 탭 이동 증가 |
| 4 | 빠른 검증 숏컷 (3.5) | 재방문 시 사용성 개선 |
| 5 | 로그인 맞춤 톤·시각적 위계 (3.7, 3.8) | 만족도·일관된 인상 |

---

## 5. 개선 후 홈 블록 순서 (비로그인 예시)

1. **인사말**  
   - "게스트"  
   - "견적서만 넣으면 적정 가격을 바로 확인해드려요"
2. **메인 CTA**  
   - [견적서 검증하기] (큰 버튼)  
   - "사진 촬영·앨범·직접 입력 중 선택해서 약 1분 안에 확인할 수 있어요"
3. **로그인 유도**  
   - "검증 결과를 저장하고 내역을 관리하려면"  
   - "로그인 없이도 검증은 할 수 있어요"  
   - [로그인] [회원가입]

(로그인 시에는 기존처럼 인사말 → 차량 or 차량 등록 CTA → 메인 CTA → 최근 검증 내역 + "전체 보기"로 두면 됨.)

---

## 6. 정리

- **비로그인**: "검증 먼저, 로그인은 선택"이 드러나도록 **순서와 문구**를 조정.
- **로그인**: 차량 미등록 시 **다음 행동(차량 등록)**을 버튼으로 제공.
- **공통**: 메인 CTA를 **첫 번째 행동**으로 두고, **기대치(방식·소요 시간)**를 한 줄로 제시.
- **재방문**: **빠른 검증 숏컷**과 **최근 내역 전체 보기**로 이탈 줄이고 내역 탭 사용을 늘림.

이 순서대로 적용하면 유저 플로우(첫 검증 → 저장·내역·차량)와 홈 구조가 잘 맞게 됨.
